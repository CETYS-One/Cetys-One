import { Box, Text } from "native-base";
import { SafeAreaView } from "react-native-safe-area-context";

const Cart = () => {
  return (
    <SafeAreaView>
      <Box>
        <Text>Cart</Text>
      </Box>
    </SafeAreaView>
  );
};

export default Cart;
