import {Text, Box} from 'native-base';
import Section from './Section';

const MainSection = () => {
  return(
    <Box>
      <Section/>
      <Section/>
      <Section/>
    </Box>
    
  );
}

export default MainSection;